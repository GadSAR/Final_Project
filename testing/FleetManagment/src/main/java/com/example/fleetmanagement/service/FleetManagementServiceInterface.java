package com.example.fleetmanagement.service;

import com.example.fleetmanagement.entity.Info;

import java.util.List;

public interface FleetManagementServiceInterface {
    List<Info> getAllStudents();
 /*   Info saveStudent(Info info, MultipartFile file);

    Info getStudentById(Long id);

    Info updateStudent(Long id, Info info, MultipartFile file, boolean isANewImageFile);

    void deleteStudent(Long id);*/
}
